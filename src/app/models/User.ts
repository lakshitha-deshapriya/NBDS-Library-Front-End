export class User {
  name: number;
  username: String;
  role: String;

  constructor(name: number, username: String, role: String) {
    this.name = name;
    this.username = username;
    this.role = role;
  }
}
